import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    menu: {
        left: 100,
        top: -30,
        paddingTop: "10%",
        fontSize: 40,
        position: "absolute",
        color: "red",
    },
});

export default styles